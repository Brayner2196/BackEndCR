package com.backendcr.residentialcomplex.entity.enums;

/**
 * Forma de combinar las condiciones dentro de una regla de cartera.
 * AND = deben cumplirse todas; OR = basta con que se cumpla una.
 */
public enum OperadorLogico {
    AND,
    OR
}
